name: invee_flutter
description: "Helper app for project Invee."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  shared_preferences: ^2.5.5
  flutter_inappwebview: ^6.1.5
  flutter_secure_storage: ^9.0.0
  image_picker: ^1.2.3
  mobile_scanner: ^7.2.0
  app_links: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
